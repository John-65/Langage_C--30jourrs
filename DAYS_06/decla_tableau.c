#include <stdio.h>


int main(){
    // Les façon de déclarer un tableau
    int notes[3]; //Déclaration simple (taille fixe)

    int vie[5] = {10, 12};//Déclaration + initialisation partielle
    int prix[] = {10, 12, 15, 18};//Déclaration sans préciser la taille(Le compilateur calcule automatiquement la taille (ici 4).)
    int note [5] = {0};//Initialisation de  tout à 0
}